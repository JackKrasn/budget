Доработки фронтенда для детализированного планирования бюджета
Текущее состояние бэкенда
Структура данных

budget_items (лимит на категорию)
├── planned_amount = SUM(planned_expenses) + buffer_amount
├── buffer_amount (резерв на незапланированные траты)
└── planned_expenses_sum (сумма конкретных покупок)

planned_expenses (конкретные покупки)
├── name: "Роутер", planned_amount: 20000
├── name: "Ипотека Сбер - платёж #13", planned_amount: 65000
└── status: pending | confirmed | skipped
API эндпоинты
Метод	URL	Описание
GET	/budgets/{id}	Бюджет с items, plannedExpenses, summary
POST	/budgets/{id}/items	Создать/обновить лимит категории (с bufferAmount)
POST	/budgets/{id}/generate-planned	Генерация из recurring → автосоздание budget_items
POST	/budgets/{id}/generate-credit-payments	Генерация из кредитов → автосоздание budget_items
GET	/planned-expenses?budgetId=...	Список запланированных покупок
POST	/planned-expenses	Добавить конкретную покупку
POST	/planned-expenses/{id}/confirm-with-expense	Подтвердить с реальной суммой
POST	/planned-expenses/{id}/skip	Пропустить покупку
UI компоненты для доработки
1. Карточка категории бюджета (BudgetCategoryCard)
Текущее отображение:

┌─────────────────────────────────────┐
│ 🏠 Кредиты                    План: │
│                              65 000 │
│ ████████░░░░░░░░░   Факт: 58 000   │
└─────────────────────────────────────┘
Новое отображение с детализацией:

┌─────────────────────────────────────────────────┐
│ 🏠 Кредиты                                      │
│                                                 │
│ Запланированные покупки:           123 000 ₽   │
│   • Ипотека Сбер - платёж #13      65 000 ₽ ✓  │
│   • Ипотека ВТБ                    58 000 ₽ ⏳  │
│                                                 │
│ Буфер на непредвиденное:            10 000 ₽   │
│ ─────────────────────────────────────────────── │
│ Итого лимит:                       133 000 ₽   │
│ Потрачено:                          58 000 ₽   │
│ Остаток:                            75 000 ₽   │
│                                                 │
│ ████████████░░░░░░░░░░░░░░  44%                │
│                                                 │
│ [+ Добавить покупку]  [✏️ Изменить буфер]      │
└─────────────────────────────────────────────────┘
Данные из API (GET /budgets/{id}):

interface BudgetItemWithCategory {
  id: string;
  categoryId: string;
  categoryName: string;
  categoryIcon: string;
  categoryColor: string;
  plannedAmount: number;      // Итоговый лимит (сумма + буфер)
  bufferAmount: number;       // Буфер
  plannedExpensesSum: number; // Сумма конкретных покупок
  actualAmount: number;       // Фактические траты
  remaining: number;          // Остаток
}

// plannedExpenses в том же ответе
interface PlannedExpense {
  id: string;
  budgetId: string;
  categoryId: string;
  name: string;
  plannedAmount: number;
  status: 'pending' | 'confirmed' | 'skipped';
  plannedDate: string | null;
}
2. Модалка добавления покупки (AddPlannedExpenseModal)

┌─────────────────────────────────────────────────┐
│           Добавить запланированную покупку      │
├─────────────────────────────────────────────────┤
│                                                 │
│ Категория:  [🏠 Кредиты          ▼]            │
│                                                 │
│ Название:   [Новый роутер____________]         │
│                                                 │
│ Сумма:      [20 000] ₽                         │
│                                                 │
│ Дата:       [15.01.2026] (необязательно)       │
│                                                 │
│ Заметка:    [_______________________]          │
│                                                 │
│         [Отмена]        [✓ Добавить]           │
└─────────────────────────────────────────────────┘
API вызов:

POST /planned-expenses
{
  "budgetId": "uuid",
  "categoryId": "uuid", 
  "name": "Новый роутер",
  "plannedAmount": 20000,
  "plannedDate": "2026-01-15",  // опционально
  "notes": "..."                // опционально
}
Результат: budget_item.planned_amount автоматически пересчитается.
3. Модалка подтверждения покупки (ConfirmExpenseModal)

┌─────────────────────────────────────────────────┐
│              Подтвердить покупку                │
├─────────────────────────────────────────────────┤
│                                                 │
│ Покупка:    Новый роутер                        │
│ План:       20 000 ₽                            │
│                                                 │
│ ┌─────────────────────────────────────────────┐ │
│ │ Реальная сумма: [18 500] ₽                  │ │
│ │ 💰 Экономия: 1 500 ₽                        │ │
│ └─────────────────────────────────────────────┘ │
│                                                 │
│ Счёт:       [💳 Тинькофф          ▼]           │
│                                                 │
│ Дата:       [06.01.2026]                        │
│                                                 │
│         [Отмена]        [✓ Подтвердить]        │
└─────────────────────────────────────────────────┘
API вызов:

POST /planned-expenses/{id}/confirm-with-expense
{
  "actualAmount": 18500,    // если отличается от плана
  "accountId": "uuid",      // опционально
  "date": "2026-01-06"      // опционально, по умолчанию сегодня
}
4. Модалка настройки буфера (SetBufferModal)

┌─────────────────────────────────────────────────┐
│            Настроить буфер категории            │
├─────────────────────────────────────────────────┤
│                                                 │
│ Категория:  🛒 Продукты                         │
│                                                 │
│ Запланированные покупки:         45 000 ₽      │
│                                                 │
│ Буфер на непредвиденное:                        │
│ [10 000] ₽                                      │
│                                                 │
│ ─────────────────────────────────────────────── │
│ Итоговый лимит:                  55 000 ₽      │
│                                                 │
│         [Отмена]        [✓ Сохранить]          │
└─────────────────────────────────────────────────┘
API вызов:

POST /budgets/{budgetId}/items
{
  "categoryId": "uuid",
  "bufferAmount": 10000
  // plannedAmount вычисляется автоматически
}
5. Кнопка генерации автоплатежей
На странице бюджета добавить кнопки:

┌─────────────────────────────────────────────────┐
│ Бюджет: Январь 2026                             │
│                                                 │
│ [🔄 Сгенерировать из шаблонов]                 │
│ [💳 Добавить платежи по кредитам]              │
│                                                 │
│ После генерации автоматически создадутся       │
│ категории с лимитами                           │
└─────────────────────────────────────────────────┘
API вызовы:

// Из повторяющихся расходов (ипотека, аренда, подписки)
POST /budgets/{id}/generate-planned

// Из графиков кредитов
POST /budgets/{id}/generate-credit-payments
6. Сводка по planned_expenses (PlannedExpensesSummary)

┌─────────────────────────────────────────────────┐
│ 📋 Запланированные расходы                      │
├─────────────────────────────────────────────────┤
│                                                 │
│ Всего запланировано:           245 000 ₽       │
│ ├── Ожидают:          ⏳       120 000 ₽  (5)  │
│ ├── Подтверждено:     ✓        115 000 ₽  (8)  │
│ └── Пропущено:        ⏭️        10 000 ₽  (2)  │
│                                                 │
│ 💰 Экономия:                     3 500 ₽       │
│ (купили дешевле чем планировали)               │
│                                                 │
└─────────────────────────────────────────────────┘
Данные из API (в ответе GET /budgets/{id}):

interface PlannedExpensesSummary {
  totalPlanned: number;    // Всего запланировано
  totalConfirmed: number;  // Подтверждено
  totalPending: number;    // Ожидает
  totalSkipped: number;    // Пропущено
  totalSavings: number;    // Экономия (план - факт)
  pendingCount: number;
  confirmedCount: number;
  skippedCount: number;
}
Пример полного flow
Сценарий: Планирование покупок на месяц
Создаём бюджет → POST /budgets
Генерируем автоплатежи → POST /budgets/{id}/generate-planned
Создаются planned_expenses из recurring (ипотека, аренда)
Автоматически создаются budget_items для категорий
Добавляем кредитные платежи → POST /budgets/{id}/generate-credit-payments
Создаются planned_expenses из credit_schedule
budget_items обновляются
Добавляем конкретные покупки → POST /planned-expenses
"Роутер" в категорию "Электроника" — 20000
budget_item.planned_amount автоматически растёт
Устанавливаем буфер → POST /budgets/{id}/items
bufferAmount: 10000 на "Электронику"
Итого лимит: 20000 + 10000 = 30000
Совершаем покупку → POST /planned-expenses/{id}/confirm-with-expense
actualAmount: 18500 (дешевле плана)
Экономия: 1500
Смотрим результат → GET /budgets/{id}
totalSavings: 1500
Категория "Электроника": план 30000, факт 18500, остаток 11500